<% if form_object.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(form_object.errors.count, "error") %> prohibited this <%= form_object.model_name.human(count: 1) %> from being saved:</h2>

    <ul>
    <% form_object.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% form_object.fields.each do |field| %>
  <div class="field">
    <%= render(partial: field, as: :field, locals: { form: form }) %>
  </div>
<% end %>

<div class="actions">
  <%= form.submit %>
</div>
