<% if errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(errors.count, "error") %> prohibited this <%= model_name.human(count: 1) %> from being saved:</h2>

    <ul>
    <% errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% fields.each do |field| %>
  <div class="field">
    <%= render field, form: form %>
  </div>
<% end %>

<div class="actions">
  <%= form.submit %>
</div>
